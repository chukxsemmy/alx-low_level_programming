#include "main.h"

/**
 *add - sums up the value of two numbers or digits
 *Return: returns the sum
 *@x: takes the value of the first integer
 *@y: takes the value of the second integer
 */

int add(int x, int y)
{
	return (x + y);
}

