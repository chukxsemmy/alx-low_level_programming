#include "main.h"

/**
 *_strlen - length of a string
 *@s: string to be evaluated
 *Return: lenght;
 */

int _strlen(char *s)
{
	int n;

	n = 0;
	int count;

	for (count = 0; s[count] != '\0'; count++)
		inc++;

	return (inc);
}
